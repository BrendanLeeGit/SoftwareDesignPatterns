package MVC;

public class Notes {
    /*
    USER uses CONTROLLER manipulates MODEL updates VIEW displaysTo USER
    the assignment
    Learn Swing
    use the program for csv into mvc, but map shit to swing components
    have text box that displays stuff, buttons with clear,
    button with plot and export
    controls in its own world, data in its own world, and display in its own world
     */
}
